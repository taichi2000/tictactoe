group 'com.nisum.secretmeetings'
version '1.0-SNAPSHOT'

apply plugin: 'java'

defaultTasks = ["clean", "test"]

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
